# 常见的数据结构
- array 连续内存固定长度
- map  健值对 数组
- list 混合类型容器
# 关系数据库

每个字段都连续增加 array 增长版

字段组成list

删除和update--直接替换 只不过一个 删除是替换为null update替换成有效值  不允许数据设置null或者设置null就是删除

查找

插入 就在array后面添加

不过一般都是按照固定内存尺寸分配的

固定几个array 管理array 也是内存块  对应实际硬件的分配  实际硬件分配职能 替换整个  512  程序是  12 或者1
ssd和机械硬盘都是最小读写单元
ssd（Die，Plane，Block，Page） 最小是page一般为2KB/4KB/8KB/16KB
机械硬盘是盘片 扇区 最小是扇区 512kb

当然这些最小内存单元读写被操作系统隔离了。都是操作系统来管理   统一为页
